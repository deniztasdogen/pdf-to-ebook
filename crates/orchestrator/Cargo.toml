[package]
name = "pdftomobi-orchestrator"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pdftomobi-core    = { workspace = true }
pdftomobi-extract = { workspace = true }
pdftomobi-ebook   = { workspace = true }
anyhow = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# Only so a test can look inside the EPUB it just built.
zip = { version = "2", default-features = false, features = ["deflate"] }
