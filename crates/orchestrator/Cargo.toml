[package]
name = "pdf-to-ebook-orchestrator"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pdf-to-ebook-core    = { workspace = true }
pdf-to-ebook-extract = { workspace = true }
pdf-to-ebook-ebook   = { workspace = true }
anyhow = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# Only so a test can look inside the EPUB it just built.
zip = { version = "2", default-features = false, features = ["deflate"] }
