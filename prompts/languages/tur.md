# Turkish

Fault classes that need Turkish to state, measured on a 419-page Turkish scan.
They are spliced into the general list in `../proofread.md`, at the
`language-rules` slot, and numbered on from wherever it ends.

The last entry is not a rule but the measured examples of the *general* classes
as that scan renders them. They were part of the tuned prompt and are kept,
because they are the evidence the numbers in `docs/findings.md` §4.5 were
produced with — they simply have no business being shown to an English book.

## label

Turkish

## rules

- Lost `ğ`. A capital `A` inside a lowercase word is a `ğ` that failed to
  scan; restore it with the vowels the word needs (girdiAfde -> girdiğinde,
  SoluAum -> Soluğum, canının yandıAı -> canının yandığı).
- `m` is also misread as `ın` or `nı` (sarınısaklı -> sarımsaklı), and `rı`
  collapses to `n` (Notlannda -> Notlarında, yanyor -> yarıyor).
- A case suffix on a name takes an apostrophe, and the name itself is often
  misread (Carlanm -> Carla'nın, Cariayı -> Carla'yı, Brooke,u -> Brooke'u,
  ClaphamCiaki -> Clapham'daki).
- A sentence starting with a lowercase `i` starts with `İ` (istemsizce ->
  İstemsizce, "insanlar -> "İnsanlar).
- Examples of the faults above, as this scan renders them: girdim:' ->
  girdim.", temizlerneye -> temizlemeye, Dikatle -> Dikkatle, ayakbı ->
  ayakkabı, müve.kler -> müvekkiller, belieğimi -> belleğimi, bağcıldı ->
  bağcıklı, hanyoda -> banyoda, ikna ebneye -> ikna etmeye, Sabrı m -> Sabrım,
  tuhafbir -> tuhaf bir, oldu�nu -> olduğunu, Halin�e -> Halinde, ç�dığının ->
  çıkardığının, koştu� -> koştu.
